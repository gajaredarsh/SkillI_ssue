# Restored test outputs go here

This folder should contain your model's actual output on the test set -- the
restored images `evaluate.py` produces when run against your test `NoisyLR`
directory.

Generate them with:

    python evaluate.py --input_dir /path/to/test/NoisyLR --output_dir outputs/

Delete this placeholder file once the real restored outputs are in place.
